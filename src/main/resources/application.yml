server:
  port: 8080  # ??????

spring:
  application:
    name: netdisk-backend

  # ???????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # ??? netdisk ???????????
    url: jdbc:mysql://localhost:3306/netdisk?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root    # ??????????? root
    password: dqf114514***

  # Redis ?? (?????????)
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

# MyBatis-Plus ??
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # ??????? SQL?????
  global-config:
    db-config:
      logic-delete-field: deleted # ??????